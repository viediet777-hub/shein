[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python main.py"
healthcheckPath = ""
healthcheckTimeout = 100

[run]
# Data persistence ke liye Railway Volume mount karo aur env set karo:
# VIEDIET_DATA_DIR=/data
# VIEDIET_BOT_TOKEN=apna_token
